GRAPHICS  (export these as TRANSPARENT PNGs — alpha on, background deleted)
==========================================================================
webbanner.png   -> full-width banner at the very top.
WebButton.png   -> the CLICK HERE FOR TIX! button at the bottom (links to GalaBid).

Why alpha: the page background is ice-green (#DCE9EA). A solid (black/white)
background baked into the PNG shows up as a box. Transparent PNGs let the green
show through, so the art floats cleanly with no edges to match.
